import PropTypes from 'prop-types'
import React, { memo } from 'react'

const AppFooter
 = memo((props) => {
  return (
    <div>AppFooter
      
    </div>
  )
})

AppFooter
.propTypes = {}

export default AppFooter
